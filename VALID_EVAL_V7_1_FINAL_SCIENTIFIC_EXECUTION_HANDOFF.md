# ValidEval V7.1 Final Scientific-Execution Handoff

## 1. Executive verdict

The V7.1 implementation is scientifically execution-ready once the canonical source tag and CI
reseal recorded in the machine state pass. This means the software boundary is ready; it does not
mean the empirical Study-C stages are authorized or complete.

## 2. Deep-audit issues received

The closure addressed source identity, runner/import incompatibility, swallowed OOMs, threshold
semantics, raw-N misuse, false simultaneous-rank labels, pairwise multiplicity, synthetic protocol
truthfulness and determinism, power misalignment, Study-H estimands/nulls, transport provenance,
family leakage, human totals, forensics, and canonical/deduplicated estimands.

## 3. P0 repairs completed

All ten P0 implementation issues are closed and tested. The detailed mapping is in the V7.1 deep
audit ledger. No empirical result was invented to close an implementation gate.

## 4. P1 repairs completed

Study-H estimands, null contracts, Monte Carlo sweeps, fold provenance, grouped cross-fit, human
costing, and manifest-bounded forensics are repaired. Generalizability and measurement-regime work
are explicitly supporting-only. BBH estimand comparison remains blocked by missing responses.

## 5. New upgrades added

Known-truth claim calibration, family-dependence stress, selective-decision observations,
simultaneous-rank coverage, eight-estimand power planning, sequential stage authorization, and a
throughput-dependent compute optimizer were added.

## 6. V7 negative results preserved

`V7_SYNTHETIC_PRIMARY_GRID_FAILED_AND_PRESERVED`. The V7.1 controls are separate and exploratory;
they do not replace the frozen result.

## 7. Runner/import end-to-end proof

A real mock-backed runner creates 1,600 prediction rows for 200 items × eight models, packages a
directory and ZIP, and imports both through the canonical V7.1 schema. Resume and all declared
tamper/mismatch failure paths are covered.

## 8. Claim semantic repairs

Threshold, direction, units, equality behavior, claim family, analysis phase, and primary/secondary
metadata are explicit. Missing mandatory evidence abstains.

## 9. Effective-N design

Inferential unit, effective N, independence unit, and dependence structure are required. Raw N is
audit metadata and cannot license an inferential claim by itself.

## 10. Rank inference

Only joint max-deviation bootstrap rank sets can license top-k claims. Marginal rank intervals are
separate. Known-truth joint coverage was at least 0.985 in the six declared scenarios.

## 11. Multiplicity

Study-H all-pairs inference uses BH FDR as primary and Holm FWER as sensitivity across 741 pairs.

## 12. Synthetic protocol accounting

All declared controls are implemented in a V7.1 module/config. Metrics are tie-invariant and each
row states its non-confirmatory role.

## 13. Synthetic determinism

Repeated normalized outputs hash to
`4197a50279184d7e624820af991800b473484ebedd0abdaefecca1339abfefbe`.

## 14. Null models

Seven machine-readable contracts state what is fixed, estimated, randomized, and hypothesized.
Study-H inference remains null-model-sensitive.

## 15. Study-H estimands

Canonical item-weighted and balanced subject-weighted ranks have Spearman 0.9962 and the same
winner in the cached 39-model panel. Both estimands remain separately labeled.

## 16. Generalizability

`GENERALIZABILITY_REMAINS_SUPPORTING_ONLY`; no population variance-component claim is made.

## 17. Measurement regime

`MEASUREMENT_REGIME_SUPPORTING_ONLY`; the deterministic V7 regime grid is not promoted to a main
psychometric reliability contribution.

## 18. Transport fold provenance

Twelve hashed fold specifications exist. They are planned. Only executed folds with hashed data
artifacts pass licensing, so empirical transport is blocked.

## 19. Cross-fitting

Discovery and held-out model sets are grouped by family. The cached MMLU analysis records zero
family overlap in every policy.

## 20. Human planning

Four-stratum unique-item, raw-label, annotator, and adjudication totals are corrected. No human
labels are claimed.

## 21. Benchmark forensics

Exact and normalized manifest checks are complete. Text-dependent near-duplicate and semantic
checks are unavailable from the frozen manifests. No contamination conclusion is permitted.

## 22. Canonical vs deduplicated analysis

MMLU was compared at 14,042 versus 14,015 items; winner and top five did not change. BBH remains
blocked by its missing exact response matrix.

## 23. Power redesign

The planner covers eight estimands and active design dimensions. No stage has all primary
estimands at 0.80 power, so a minimum-scientific label is not permitted from this grid.

## 24. GPU compute optimization

The optimizer is implemented but returns `COMPUTE_OPTIMIZATION_BLOCKED_PENDING_S2` until measured
accepted T4 throughput is supplied.

## 25. Source provenance

All execution configs pin `valideval-v7.1-icml2027-scientific-execution-ready`; exact tagged commit
and hashes are recorded in the machine state after the deterministic reseal.

## 26. Tests and CI

Local tests, Ruff lint/format, critical-module mypy, notebook fixtures, artifact validation, and
sdist/wheel builds are required to pass. The exact counts and CI run are recorded in machine state.

## 27. Final execution authorization

- S1: `S1_BLOCKED` — no real T4 preflight.
- S2: `S2_BLOCKED` — no accepted S1, throughput, or extraction evidence.
- S3: `S3_BLOCKED_PENDING_S2`.
- S4: `S4_BLOCKED_PENDING_S3`.

## 28. Exact next action

Run the frozen S1 T4 preflight at the canonical tag, import its ZIPs through the V7.1 importer, and
accept or reject S1 before collecting S2 or optimizing later stages.

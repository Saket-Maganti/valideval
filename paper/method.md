# Method

ValidEval models benchmark auditing as a multidimensional validity profile rather than a scalar
validity score. Diagnostics are treated as instruments that need calibration and limitations.

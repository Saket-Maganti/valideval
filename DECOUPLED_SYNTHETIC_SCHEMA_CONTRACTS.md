# Decoupled Synthetic Schema Contracts

All future decoupled synthetic runs remain `RESULT_REQUIRED` until real guard-passing artifacts exist. Planned manifests are not paper evidence. Hidden labels/flaw fields must not reach model predictions or diagnostic readouts.

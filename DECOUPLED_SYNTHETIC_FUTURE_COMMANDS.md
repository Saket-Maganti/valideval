# Decoupled Synthetic Future Commands

`RESULT_REQUIRED` before any claim promotion. Planned command:

```bash
python3 -m valideval decoupled-synthetic-preflight --dry-run
```

If a future execute command exists and guards pass, run it only as a new result lane; planned commands are not paper evidence.
